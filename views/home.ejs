<div class="container">
	<h1>Oh Hello</h1>
	<h2>Check out some movie I like</h2>
	<%console.log('hi')%>

	<% movies.forEach(function(m){%>
		<div>
			<h3><%= m.Title %> (<% m.Year %>) </h3>
			<img src="<%= m.Poster %>">
		</div>
	<% }); %>
</div>